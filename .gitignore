# ----------------------------------------------------------------------
# 1. C/C++ Build Artifacts (빌드 결과물 제외)
# ----------------------------------------------------------------------
# 컴파일된 오브젝트 파일 (중간 결과물)
*.o
*.obj

# 최종 실행 파일 (Makefile에서 .out을 붙이기로 했으므로 패턴으로 차단)
*.out
*.exe
*.bin

# 디버깅 심볼 폴더 (macOS gcc/clang 사용 시 생성될 수 있음)
*.dSYM/

# (옵션) 혹시 확장자 없는 구버전 바이너리가 남아있다면 명시적으로 제외
/mytop

# ----------------------------------------------------------------------
# 2. Editor & IDE Settings (개인 개발 환경 설정 제외)
# ----------------------------------------------------------------------
# VS Code 설정 폴더 (협업 시 개인 설정 충돌 방지)
.vscode/

# VS Code 히스토리/임시 파일
.history/

# ----------------------------------------------------------------------
# 3. OS System Files (운영체제 자동 생성 파일 제외)
# ----------------------------------------------------------------------
# macOS 폴더 아이콘/속성 저장 파일 (맥북 사용자 필수 제외 항목)
.DS_Store

# Windows 썸네일 캐시
Thumbs.db

# ----------------------------------------------------------------------
# 4. Others (기타)
# ----------------------------------------------------------------------
# 백업 파일 (vim이나 에디터가 자동 생성하는 물결표 파일)
*~
*.swp
*.swo